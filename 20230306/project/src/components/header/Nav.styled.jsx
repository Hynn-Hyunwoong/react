import styled from "styled-components"

export const Nav = styled.Nav`
    ul {
        display: flex;
    }

    li {
        margin-left : 10px;
    }

    a {
        color : #fff;
        padding : 5px;

        a:hover {
            
        }
    }
`